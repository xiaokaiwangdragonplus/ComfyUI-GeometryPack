# Installation dependencies (for install.py optimization)
requests>=2.25.0
tqdm>=4.60.0

# Core dependencies
libigl>=2.6.1
numpy>=1.21.0
scipy>=1.7.0
trimesh>=3.15.0
pymeshlab>=2022.2
cgal>=0.9.0  # Official CGAL Python bindings for isotropic remeshing
rtree
embreex

# Visualization
matplotlib>=3.5.0
Pillow>=9.0.0
pyvista>=0.43.0

# Ray tracing (required for backdraft detection)
trimesh[easy]  # Includes embree for fast ray casting

# Distance metrics and point cloud processing
point-cloud-utils>=0.30.0
mesh-to-sdf>=0.0.14

# Mesh simplification
fast-simplification>=0.1.5

# UV unwrapping
xatlas>=0.0.11

# Skeleton extraction (optional)
skeletor>=1.2.0
PyNanoInstantMeshes